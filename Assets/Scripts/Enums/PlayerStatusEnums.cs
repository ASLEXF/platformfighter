public enum LifeStatusEnum
{
    Alive = 0,
    Dead = 1,
}

public enum DebuffEnum
{
    Normal = 0,
    Stunned = 1,
    Frozen = 2
}

public enum BuffEnum
{
    Normal = 0,
    PowerBoost = 1,
    HealthBoost = 2,
}